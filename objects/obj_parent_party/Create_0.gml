// obj_parent_party Create Event
if (object_index == obj_parent_party) {
    speed = 4;
    health = 500; // Explicitly set health value for obj_parent_party
    show_debug_message("Parent Party Created with health: " + string(health));
}
